return {
    wall = function(cutscene)
        -- Open textbox and wait for completion
        cutscene:text("* The wall seems cracked.")
        cutscene:text("* . . .")
        cutscene:text("[color:red]* They don't know\n[wait:6]what's about to happen...[color:reset]")
    end
    
}
